/*global require, describe, it */
const assert = require('assert');
const mojl = require('../../mojl.js');

describe('Test Defaults', () => {

	it('defaults should work', () => {
		assert(true);
	});

});
