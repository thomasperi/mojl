Add dev-loader templates for CSS and SASS

That'll probably involve using a real template engine instead of just replacing a single token, so...

Add a templating feature, where modules can `include` one another and you can build a whole site with it.

Allow images to be nested in images directories